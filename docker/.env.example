# Hugging Face token (required for speaker diarization)
# Create at https://huggingface.co/settings/tokens
# Accept terms at https://huggingface.co/pyannote/speaker-diarization-3.1
HF_TOKEN=hf_xxxxxxxxxxxx

# GitHub token (for cloning private repo when REPO_URL is set)
# Create at https://github.com/settings/tokens (repo scope for private repos)
# Leave empty if using public repo or SSH
GITHUB_TOKEN=

# Repo URL to clone at runtime for latest code (optional)
# If set, the container will clone/pull this repo before running the pipeline.
# Use https://github.com/USER/REPO.git or git@github.com:USER/REPO.git
# For private repos: use GITHUB_TOKEN with HTTPS, or mount ~/.ssh for SSH
REPO_URL=
